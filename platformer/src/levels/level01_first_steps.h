/*
 * level01_first_steps.h - the intro course: a start plaza, a gentle chain
 * of platforms climbing then descending with gaps that need a jump, and a
 * goal plaza at the far end. See src/levels.h for how levels are registered.
 */
#ifndef LEVEL01_FIRST_STEPS_H
#define LEVEL01_FIRST_STEPS_H

#include "level.h"

void level01_first_steps_build(Level *lvl);

#endif /* LEVEL01_FIRST_STEPS_H */
